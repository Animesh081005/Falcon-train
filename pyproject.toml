[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "falcon-wordbox-ocr"
version = "0.1.0"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["wordbox_ocr*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
